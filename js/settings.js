(()=>{const key="kanarin-motion";const btn=document.querySelector("#motion-toggle");function apply(off){document.documentElement.classList.toggle("motion-off",off);if(btn)btn.textContent=`ANIMATION: ${off?"OFF":"FULL"}`}let off=localStorage.getItem(key)==="off"||matchMedia("(prefers-reduced-motion: reduce)").matches;apply(off);btn?.addEventListener("click",()=>{off=!off;localStorage.setItem(key,off?"off":"full");apply(off)});})();